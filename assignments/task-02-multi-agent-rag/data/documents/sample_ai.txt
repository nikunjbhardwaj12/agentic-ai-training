The Transformer architecture, introduced in 2017, completely replaced recurrent networks with attention mechanisms. The core innovation is "Self-Attention", which allows the model to process all tokens in a sequence simultaneously, capturing long-range dependencies regardless of distance. This parallelization drastically reduced training times and enabled the scaling of massive Large Language Models (LLMs).
